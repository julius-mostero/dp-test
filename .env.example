# Port the webhook server listens on (default: 3000)
PORT=3000

# GitHub webhook secret configured in your GitHub repository settings.
# Used to verify that incoming requests are genuinely from GitHub.
WEBHOOK_SECRET=your_github_webhook_secret_here
